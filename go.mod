module github.com/jaminalder/go-graphics

// Kept at 1.25 so the locally installed golangci-lint (built with go1.25)
// can typecheck the module; no 1.26 language features are used. Raise
// together with a golangci-lint upgrade.
go 1.25
