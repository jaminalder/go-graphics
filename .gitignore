# Generated images (renders go here; never commit large outputs)
/out/

# Binaries
/bin/
/staticart

# OS / editor noise
.DS_Store
*.swp

# Agent worktrees live inside the repo; they are checkouts, not content.
.claude/worktrees/
.worktrees/

# Public runtime/deployment secrets, state and browser tooling
web/browser/node_modules/
web/browser/test-results/
web/browser/playwright-report/
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
backend.hcl
crash.log
.env
